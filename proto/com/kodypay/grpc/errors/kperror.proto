enum KpError {
    // INVALID_ARGUMENT family (parent gRPC code 3)
    INVALID_ARGUMENT_GENERIC = 300;
    INVALID_ARGUMENT_USERNAME = 301;

    // DEADLINE_EXCEEDED family (parent gRPC code 4)
    DEADLINE_EXCEEDED_GENERIC = 400;
    DEADLINE_EXCEEDED_PAYMENT = 401;
}