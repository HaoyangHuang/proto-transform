{
  "name": "server",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "install-deps": "dotenv -e .env -- pnpm install",
    "postinstall": "./script/postinstall.sh",
    "build": "node esbuild.config.js --watch"
  },
  "dependencies": {
    "protobufjs": "^7.5.3"
  },
  "devDependencies": {
    "@kodypay/kp-protocols-grpc": "^1.0.716",
    "@types/node": "^24.1.0",
    "esbuild": "^0.25.8"
  }
}
